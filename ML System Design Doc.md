## 1. Цели и предпосылки

### 1.1. Зачем идем в разработку продукта?

| Роль | Ответ |
| :--- | :--- |
| **Бизнес-цель (Product Owner)** | Снизить операционные потери газа от невыявленных утечек на **15%** и предотвратить техногенные аварии за счет перехода от планово-предупредительного ремонта к предиктивной аналитике. |
| **Почему станет лучше (ML)** | **Сейчас:** обход трасс бригадами раз в 2 недели, реагирование только на падение давления.<br>**Будет:** обнаружение микро-утечек (от 0.1 л/с) в реальном времени с локализацией до **50 метров**. Время реакции снизится с дней до минут. |
| **Успех итерации (PO)** | Precision **> 95%**, Recall **> 90%** на тестовом полигоне длиной 50 км в течение 3 месяцев без ложных срабатываний более 1 раза в неделю. |

### 1.2. Бизнес-требования и ограничения

**Бизнес-требования:**
- Соответствие ПБ 12-529-03 «Правила безопасности систем газораспределения»
- Передача сигнала в диспетчерскую ЦДС за **< 5 секунд** с момента события

**Бизнес-ограничения:**

> [!CAUTION]
> - **Запрет** на использование любых иностранных облачных серверов для обработки данных (только локальный контур или ГосСОПКА)
> - Энергопотребление полевого датчика **< 2 Вт** (работа от солнечной панели)
> - Работоспособность при температуре **от -55°C до +45°C**

**Критерии успеха пилота:**
- Модель обнаружила **3 из 4** искусственно созданных утечек на полигоне
- Частота ложных срабатываний **≤ 1 в неделю** на 10 км трассы

### 1.3. Скоуп проекта

**Входит в скоуп (DS):**
- Бинарный классификатор (утечка / шум)
- Модель регрессии для локализации места утечки (TDOA)
- Пайплайн предобработки сигнала
- One-Class SVM для обнаружения неизвестных типов утечек

**Что НЕ входит:**
- Разработка firmware для сенсоров (команда Embedded)
- UI диспетчерского пульта (фронт-команда)

**Технический долг:**
- Отсутствие механизма Continual Learning
- Хардкод частоты дискретизации (20 кГц)

### 1.4. Предпосылки решения

**Используемые блоки данных:**

| Данные | Источник | Статус |
| :--- | :--- | :--- |
| Акустические записи (размеченные) | Полигон 2022-2023 | Требуется реразметка |
| Параметры трубы (давление, диаметр) | SCADA-архив | ✅ Доступно |
| Метеоданные (ветер, осадки) | API Росгидромет | ✅ Доступно |
| Исторические акты аварий | Журнал ЦДС | ⚠️ Требуется допуск |

**Гипотезы:**
1. Спектр утечки газа имеет характерные гармоники в диапазоне **5-15 кГц**
2. Разница во времени прихода сигнала (TDOA) позволяет локализовать утечку с точностью **±50 м**

> [!IMPORTANT]
> **Вопрос бизнесу:** Насколько отличается акустический портрет утечки при низком давлении (5 атм) vs высоком (100 атм)?

---

## 2. Методология Data Scientist

### 2.1. Постановка задачи

Гибридная система, объединяющая три типа задач ML:

| Тип задачи | Применение |
| :--- | :--- |
| **Бинарная классификация** | Определение: есть утечка газа (1) или нет (0) |
| **Обнаружение аномалий** | Выявление неизвестных типов дефектов (бейзлайн) |
| **Регрессия (локализация)** | Оценка расстояния от сенсора до места утечки |

**Формат вывода (на окне 1 секунда):**
- `p_leak ∈ [0,1]` — вероятность утечки
- `distance ∈ [0, 5000]` метров — расстояние до источника

### 2.2. Блок-схема решения

#### Бейзлайн (MVP 1.0)

```mermaid
flowchart TD
    A[Сырые аудиозаписи] --> B[Предобработка<br>фильтрация + сегментация]
    B --> C[Feature Engineering<br>MFCC + спектральные признаки]
    C --> D[Обучение<br>Random Forest / One-Class SVM]
    D --> E[Оценка на тестовом полигоне]
    E --> F{Precision > 0.95<br>Recall > 0.90?}
    F -- Нет --> C
    F -- Да --> G[Экспорт в ONNX<br>пилот на 2 контроллерах]

## 2.3. Этапы решения задачи

### 2.3.1 Этап 1: Подготовка данных

**Данные и сущности, на которых будет обучаться модель:**

| Название данных | Есть ли в компании (источник) | Требуемый ресурс | Проверено ли качество |
| :--- | :--- | :--- | :--- |
| Акустические записи с полигона (размеченные) | Частично (записи 2022-2023) | DS + эксперт-акустик | Нет (требуется реразметка) |
| Параметры трубы (диаметр, давление, расход) | Да (SCADA-архив) | DE | Да |
| Метеоданные (скорость ветра, осадки) | Да (API Росгидромет) | DE | Да |
| Исторические акты аварий | Да (журнал ЦДС) | Безопасник | Нет (требуется допуск) |
| Синтетические утечки | Нет (создаются) | DS + симулятор | Не применимо |

**Результат этапа:** витрина данных `LEAK_DETECTION_FEATURES` с колонками:

| Колонка | Тип | Описание |
| :--- | :--- | :--- |
| `timestamp` | datetime | Время начала окна |
| `sensor_id` | string | Идентификатор сенсора |
| `mfcc_1` ... `mfcc_13` | float | MFCC-коэффициенты |
| `spectral_centroid` | float | Спектральный центроид |
| `zcr` | float | Zero-crossing rate |
| `rms` | float | Root mean square |
| `target` | int | 0 — нет утечки, 1 — утечка |
| `wind_speed` | float | Скорость ветра (м/с) |

---

### 2.3.2 Этап 2: Формирование выборки для обучения, тестирования и валидации

**Параметры выборки:**

| Параметр | Бейзлайн | Основной MVP |
| :--- | :--- | :--- |
| Горизонт прогноза | Мгновенный (окно 1 сек) | Мгновенный (окно 1 сек) |
| Гранулярность | 1 сек (шаг 0.5 сек) | 1 сек (шаг 0.5 сек) |
| Частота пересчета модели | Ежеквартально | Ежемесячно |
| Целевая переменная | `is_leak` (0/1) | `is_leak` + `distance_to_source` (метры) |
| Способ разметки | Визуальный осмотр спектрограмм + аудиопрослушивание | Автоматическая разметка + верификация экспертом |

**Результат этапа:** Train/Validation/Test сплиты (70/15/15) с сохранением временной структуры (окна из одной записи не перемешиваются между train и validation).

---

### 2.3.3 Этап 3: Feature Engineering

| Подход | Бейзлайн | Основной MVP |
| :--- | :--- | :--- |
| Извлекаемые признаки | MFCC (13), спектральный центроид, ZCR, RMS, спектральный контраст | Log-mel спектрограмма (64 bins × 128 window) + cross-correlation между каналами |
| Нормализация | StandardScaler (per-sensor) | Instance normalization (внутри CNN) |
| Аугментация | Добавление белого шума (SNR=20dB) | Time-stretch (±10%), pitch-shift (±2 полутона) |

**Результат этапа:**

| Версия | Размерность | Формат |
| :--- | :--- | :--- |
| Бейзлайн | (N_samples, 20_features) | NumPy .npy |
| Основной MVP | (N_samples, 64, 128) | HDF5 / TFRecord |

---

### 2.3.4 Этап 4: Обучение прогнозных моделей

**Бейзлайн (Random Forest + One-Class SVM):**

| Параметр | Значение |
| :--- | :--- |
| Алгоритм (классификация) | Random Forest |
| Алгоритм (аномалии) | One-Class SVM |
| n_estimators (RF) | 200 |
| max_depth (RF) | 15 |
| class_weight (RF) | balanced |
| nu (SVM) | 0.05 |
| kernel (SVM) | RBF, gamma='scale' |
| Кросс-валидация | GroupTimeSeriesSplit (5 фолдов) |

**Основной MVP (CNN на спектрограмме):**

| Параметр | Значение |
| :--- | :--- |
| Архитектура | ResNet-18 (адаптированный под 1D) с двумя выходными хэдами |
| Вход | (batch, 64, 128) — mel-спектрограмма за 2 секунды |
| Выход 1 | Sigmoid → вероятность утечки (binary cross-entropy loss) |
| Выход 2 | Linear → расстояние до утечки (Huber loss) |
| Общий Loss | L = L_cls + 0.5 * L_loc |
| Оптимизатор | AdamW (lr=1e-3, weight_decay=1e-4) |
| Scheduler | ReduceLROnPlateau (factor=0.5, patience=3) |
| Кросс-валидация | Leave-One-Segment-Out (по участкам трассы) |

**Результат этапа:** сохраненные веса модели (.pt / .pkl) + логи обучения.

---

### 2.3.5 Этап 5: Интерпретация моделей (согласование с заказчиком)

| Метод | Для чего | Формат вывода |
| :--- | :--- | :--- |
| SHAP | Бейзлайн — определение важности частотных признаков | Summary plot, Force plot |
| Grad-CAM | Основной MVP — визуализация активируемых областей спектрограммы | Тепловая карта на спектрограмме |
| Confusion Matrix | Оба подхода — анализ структуры ошибок | Матрица 2×2 |

**Бизнес-проверка:** эксперт-акустик подтверждает, что важные области спектра (по SHAP/Grad-CAM) соответствуют физике утечки газа (спектр свиста в диапазоне 5-15 кГц).

**Результат этапа:** отчет с визуализациями + заключение эксперта.

---

### 2.3.6 Этап 6: Интеграция бизнес-правил для расчета бизнес-метрик качества модели

**Бизнес-правила поверх ML-предсказания:**

| № | Правило | Обоснование |
| :--- | :--- | :--- |
| 1 | Утечка регистрируется при 3 последовательных окнах с вероятностью > 0.7 | Исключение случайных выбросов |
| 2 | При давлении в трубе < 5 атм порог снижается до 0.5 | Микроутечки опаснее при низком давлении |
| 3 | В радиусе 500 м от компрессорной станции модель отключается | Постоянный высокий шум от КС |
| 4 | При расчетной утечке > 100 м³/час (по падению давления) → автоматический вызов МЧС | Крупная авария |

**Вопрос бизнесу:** согласованы ли пороги (0.7, 0.5, 3 окна) с регламентом ПБ 12-529-03?

**Результат этапа:** функция постобработки, применяемая к выходам ML-модели перед отправкой алерта в ЦДС.

---

### 2.3.7 Этап 7: Разработка оптимизатора (выбор оптимальной итерации)

**Перебираемые параметры:**

| Параметр | Диапазон | Шаг |
| :--- | :--- | :--- |
| Порог вероятности p_threshold | [0.5, 0.9] | 0.05 |
| Длительность подтверждения windows_required | [1, 5] | 1 |

**Целевая функция (максимизация):**

`Score = F1_score - 0.1 * FAR`

где FAR (False Alarm Rate) — частота ложных срабатываний в час.

**Бизнес-калибровка:**

| Сценарий | Стоимость |
| :--- | :--- |
| Ложный вызов (FP) | 50 000 руб (выезд бригады) |
| Пропущенная утечка (FN) | 10 000 000 руб (риск аварии) |

**Оптимальная точка (расчетная):** Recall 0.95 при Precision 0.85.

**Результат этапа:** оптимальная пара (p_threshold, windows_required) на валидационной выборке.

---

### 2.3.8 Этап 8: Подготовка финального отчета для бизнеса

**Структура отчета:**

1. Резюме для руководства (ключевые метрики, готовность к пилоту)
2. Метрики качества на тестовом полигоне (таблица целевых и достигнутых значений, confusion matrix)
3. Оценка экономического эффекта (предотвращенный ущерб в руб/год, затраты на ложные вызовы)
4. Заключение эксперта-акустика (физическая обоснованность модели, Grad-CAM визуализации)
5. Рекомендации по порогам срабатывания (итоговые параметры для инференса)
6. План интеграции в ЦДС (схема развертывания, требования к Edge-контроллерам)

**Ответственные:** DS (подготовка данных и аналитики) + PO (согласование выводов).

**Результат этапа:** презентация (PDF/PPT) для бизнес-заказчика.

---

### Сводная таблица этапов

| Этап | Вход | Выход | Ответственный | Длительность |
| :--- | :--- | :--- | :--- | :--- |
| 2.3.1 | Сырые аудио + SCADA | Витрина данных | DS + DE | 5 дней |
| 2.3.2 | Витрина данных | Train/Val/Test сплиты | DS | 1 день |
| 2.3.3 | Сырые данные | MFCC / спектрограммы | DS | 3 дня |
| 2.3.4 | Признаки + таргет | Обученные модели | DS | 5 дней |
| 2.3.5 | Модели | SHAP / Grad-CAM отчет | DS | 2 дня |
| 2.3.6 | Предсказания | Алерты с бизнес-правилами | DS + PO | 2 дня |
| 2.3.7 | Валидационная выборка | Оптимальные пороги | DS | 1 день |
| 2.3.8 | Все вышеперечисленное | Презентация бизнесу | DS + PO | 2 дня |
