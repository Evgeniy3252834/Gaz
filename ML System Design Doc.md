## 1. Цели и предпосылки

### 1.1. Зачем идем в разработку продукта?

| Роль | Ответ |
| :--- | :--- |
| **Бизнес-цель (Product Owner)** | Снизить операционные потери газа от невыявленных утечек и предотвратить техногенные аварии за счет перехода от плановых пеших обходов к регулярной аэроинспекции с использованием дрона, оснащенного тепловизионной камерой и лазерным газоанализатором. |
| **Почему станет лучше (ML)** | **Сейчас:** обход трасс бригадами раз в 2 недели — до 30 км/день на человека, пропуск микро-утечек, высокие трудозатраты.<br>**Будет:** дрон пролетает 50-100 км за 1 час, два сенсора работают в комплексе: камера «видит» шлейф газа, газоанализатор «подтверждает» наличие метана. Локализация утечки — до **5 метров** (привязка к видео + GPS). |
| **Успех итерации (PO)** | Precision **> 88%**, Recall **> 92%**, FAR **≤ 1 ложное срабатывание на 100 км** полета. Точность локализации **< 5 метров** на тестовом полигоне. |

### 1.2. Бизнес-требования и ограничения

**Бизнес-требования:**
- Соответствие ПБ 12-529-03 «Правила безопасности систем газораспределения»
- Автоматическая привязка обнаруженной утечки к координатам (GPS/ГЛОНАСС дрона + телеметрия)
- Формирование отчета по каждому полету: координаты утечки — фото — концентрация метана

**Бизнес-ограничения:**

> [!CAUTION]
> - **Запрет** на использование любых иностранных облачных серверов для обработки данных (только локальный контур или ГосСОПКА)
> - Вес бортового вычислителя **< 1.5 кг** (NVIDIA Jetson Orin NX — 520 г)
> - Энергопотребление всей сенсорной системы **< 100 Вт** (ограничение батареи дрона)
> - Работоспособность при температуре **от -40°C до +40°C** (арктическое исполнение опционально)
> - Время автономной работы дрона с сенсорами **≥ 40 минут**

**Критерии успеха пилота:**
- Система обнаружила **4 из 4** искусственно созданных утечек на полигоне (размеры от 0.1 до 5 л/с)
- Ложные срабатывания **≤ 1 за 100 км** полета
- Среднее отклонение локализации **≤ 5 метров** от реальных координат утечки

### 1.3. Скоуп проекта

**Входит в скоуп (DS):**
- Мультимодальная ML-модель, объединяющая два потока данных (RGB-видео + тепловизионное видео + газоанализатор)
- Детектор шлейфа газа на RGB-видео (YOLO / EfficientDet)
- Детектор температурной аномалии на тепловизионном видео (CNN)
- Пороговая фильтрация данных газоанализатора (превышение над фоном)
- Decision fusion (взвешенное голосование / логические правила)
- One-Class модель для обнаружения неизвестных типов утечек

**Что НЕ входит:**
- Разработка аппаратной части дрона и сенсоров (команда Embedded)
- Пилотирование и навигация дрона (команда БПЛА)
- UI диспетчерского пульта (фронт-команда)

**Технический долг:**
- Отсутствие механизма Continual Learning (модель переобучается на земле после сбора новых данных)
- Хардкод высоты полета (20 метров) — при изменении высоты потребуется дообучение

### 1.4. Предпосылки решения

**Используемые блоки данных:**

| Данные | Источник | Статус |
| :--- | :--- | :--- |
| Собранный датасет | Организация "BR Lab" | ✅ Доступно |
| Видео (видимый + тепловой спектр) с привязкой к координатам | Тестовые полеты | ⚠️ Требуется сбор |
| Замеры концентрации метана (лазерный газоанализатор) | Тестовые полеты | ⚠️ Требуется сбор |
| Параметры трубы (давление, диаметр) | SCADA-архив | ✅ Доступно |
| Метеоданные (ветер, осадки, температура) | API Росгидромет | ✅ Доступно |

**Гипотезы:**
1. Тепловизионная камера видит охлаждение трубы в месте утечки (эффект Джоуля-Томсона) при перепаде давления > 20 атм
2. Совместное использование двух сенсоров снижает частоту ложных срабатываний на **90%** по сравнению с любым сенсором по отдельности

> [!IMPORTANT]
> **Вопрос бизнесу:** Допустима ли постобработка данных после полета (обнаружение утечек через 2-3 часа) или требуется реальное время (алерт во время полета)? От этого зависит, ставим ли мы NVIDIA Jetson на борт или возим записи на землю.

---

## 2. Методология Data Scientist

### 2.1. Постановка задачи

Гибридная мультимодальная система, объединяющая три типа данных и три типа задач ML:

| Тип задачи | Применение |
| :--- | :--- |
| **Детекция объектов (RGB-видео)** | Поиск шлейфа газа / конденсата на фоне трубы |
| **Детекция аномалий (тепловизионное видео)** | Поиск температурной аномалии (охлаждение трубы) |
| **Бинарная классификация (газоанализатор)** | Превышение порога метана над фоном |
| **Регрессия (локализация)** | Оценка смещения утечки в пикселях / GPS-координатах |

**Формат вывода (на каждый кадр / акустическое окно):**

| Поле | Тип | Описание |
| :--- | :--- | :--- |
| `p_leak` | float ∈ [0,1] | Итоговая вероятность утечки после фьюжна |
| `latitude` | float | Широта утечки (из видео + GPS дрона) |
| `longitude` | float | Долгота утечки |
| `methane_ppm` | float | Концентрация метана (если есть замер) |
| `sensor_contributions` | dict | Вклад каждого сенсора в решение |

**Последовательность шагов:**
## 2.2. Блок-схема решения

### Бейзлайн (MVP 1.0)

**Последовательность шагов:**
```
%% --- 1. СБОР ДАННЫХ ---
subgraph S1["1. Синхронизированный захват с дрона"]

A1["Лазерный газоанализатор<br/>(ДМЛ-150С)"]
A2["OGI-камера<br/>(GasFIR UAV-M)"]
A3["RGB-камера<br/>(5 Мп)"]

B1["ppm + GPS<br/>(20 Гц)"]
B2["IR-видео 30 fps<br/>320×256"]
B3["RGB-видео 30 fps<br/>1920×1080"]

SYNC["Синхронизация по GPS таймкоду<br/>(< 100 мс)"]

A1 --> B1
A2 --> B2
A3 --> B3

B1 --> SYNC
B2 --> SYNC
B3 --> SYNC

end

%% --- 2. ПРЕДОБРАБОТКА ---
subgraph S2["2. Предобработка"]

C1["Газоанализатор<br/>• Вычитание фона<br/>• Фильтрация выбросов<br/>• Логарифмирование"]
C2["OGI<br/>• Извлечение кадров (1 fps)<br/>• Вычитание фона<br/>• Нормализация"]
C3["RGB<br/>• Resize 224×224<br/>• Нормализация<br/>• Аугментация"]

D1["ppm_scaled"]
D2["thermal_frame<br/>(224×224×1)"]
D3["rgb_frame<br/>(224×224×3)"]

C1 --> D1
C2 --> D2
C3 --> D3

end

SYNC --> C1
SYNC --> C2
SYNC --> C3

%% --- 3. FEATURE ENGINEERING ---
subgraph S3["3. Feature Engineering"]

E1["Газ<br/>• Скользящее среднее<br/>• Разность с фоном<br/>• Пик"]
E2["OGI<br/>Кадр"]
E3["RGB<br/>Кадр"]

F1["methane_features<br/>(N×3)"]
F2["thermal_tensor"]
F3["rgb_tensor"]

E1 --> F1
E2 --> F2
E3 --> F3

end

D1 --> E1
D2 --> E2
D3 --> E3

%% --- 4. ОБЩАЯ МОДЕЛЬ ---
subgraph S4["4. Обучение моделей"]

MODEL["Fusion / ML модель"]

end

F1 --> MODEL
F2 --> MODEL
F3 --> MODEL

%% --- 4b. МОДЕЛИ ПО КАНАЛАМ ---
subgraph S4b["4. Модели по каналам"]

M1["Газоанализатор<br/>• Пороговая фильтрация<br/>если ppm > 50 → 1<br/>иначе 0"]
M2["OGI<br/>CNN (ResNet18)<br/>sigmoid → p_thermal"]
M3["RGB<br/>CNN (EfficientNetB0)<br/>sigmoid → p_rgb"]

P1["p_methane<br/>(0/1)"]
P2["p_thermal<br/>(0..1)"]
P3["p_rgb<br/>(0..1)"]

M1 --> P1
M2 --> P2
M3 --> P3

end

MODEL --> M1
MODEL --> M2
MODEL --> M3

%% --- 5. FUSION ---
subgraph S5["5. Decision Fusion"]

FUSION["Взвешенное голосование"]

FORMULA["p_leak = (p_m*w_m + p_t*w_t + p_r*w_r) / (w_m + w_t + w_r)<br/>
w_m=0.50, w_t=0.35, w_r=0.15"]

FUSION --> FORMULA

end

P1 --> FUSION
P2 --> FUSION
P3 --> FUSION

%% --- 6. ОЦЕНКА ---
subgraph S6["6. Оценка"]

CHECK["Критерии:<br/>
Precision > 0.88<br/>
Recall > 0.92<br/>
FAR ≤ 1 / 100 км<br/>
Ошибка < 5 м"]

DECISION{"Все условия выполнены?"}

CHECK --> DECISION

end

FORMULA --> CHECK

%% --- ВЕТВЛЕНИЕ ---
DECISION -- "Нет" --> C1
DECISION -- "Нет" --> C2
DECISION -- "Нет" --> C3

%% --- 7. ЭКСПОРТ ---
subgraph S7["7. Экспорт"]

EXPORT["Экспорт модели:<br/>• ONNX<br/>• TensorRT<br/>• Пилотные полеты"]

end

DECISION -- "Да" --> EXPORT
```


---

### 2.3. Этапы решения задачи

### 2.3.1 Этап 1: Подготовка данных

**Данные и сущности, на которых будет обучаться модель:**

| Название данных | Есть ли в компании (источник) | Требуемый ресурс | Проверено ли качество |
| :--- | :--- | :--- | :--- |
| Акустические записи с дрона над утечками | Нет (пилотный сбор на полигоне) | DS + пилот + акустик | Нет |
| RGB-видео утечек с привязкой к GPS | Нет (пилотный сбор) | DS + пилот | Нет |
| Тепловизионное видео утечек с привязкой к GPS | Нет (пилотный сбор) | DS + пилот | Нет |
| Замеры метана (лазерный газоанализатор) + GPS | Нет (пилотный сбор) | DS + пилот | Нет |
| Фоновые данные (нет утечек) над трубой | Нет (пилотный сбор) | DS + пилот | Нет |
| Параметры трубы (диаметр, давление, глубина) | Да (SCADA-архив) | DE | Да |
| Метеоданные (скорость ветра, направление, осадки) | Да (API Росгидромет) | DE | Да |
| Синтетические акустические утечки | Нет (создаются) | DS + симулятор | Не применимо |

**Результат этапа:** мультимодальная витрина данных `DRONE_LEAK_DETECTION` с колонками:

| Колонка | Тип | Описание |
| :--- | :--- | :--- |
| `flight_id` | string | Идентификатор полета |
| `timestamp` | datetime | Время замера (синхронизировано) |
| `drone_gps_lat` | float | Широта дрона |
| `drone_gps_lon` | float | Долгота дрона |
| `drone_altitude_m` | float | Высота дрона (м) |
| `audio_mel_spec` | array (64,128) | Mel-спектрограмма за 1 секунду |
| `video_rgb_frame` | array (224,224,3) | Кадр видимой камеры |
| `video_thermal_frame` | array (224,224,1) | Тепловизионный кадр |
| `methane_ppm` | float | Концентрация метана |
| `wind_speed_ms` | float | Скорость ветра (м/с) |
| `wind_direction_deg` | float | Направление ветра (градусы) |
| `target_leak` | int | 0 — нет утечки, 1 — утечка |
| `target_gps_lat` | float | Реальная широта утечки (для обучения локализации) |
| `target_gps_lon` | float | Реальная долгота утечки |

---

### 2.3.2 Этап 2: Формирование выборки для обучения, тестирования и валидации

**Параметры выборки:**

| Параметр | Бейзлайн | Основной MVP |
| :--- | :--- | :--- |
| Горизонт прогноза | Мгновенный (окно 1 сек / 1 кадр) | Мгновенный + скользящее окно (3 кадра / 3 акустических окна) |
| Гранулярность | 1 сек (акустика), 30 FPS (видео) | 1 сек (акустика), 30 FPS (видео) |
| Частота пересчета модели | Ежеквартально | Ежемесячно |
| Целевая переменная (классификация) | `is_leak` (0/1) | `is_leak` (0/1) |
| Целевая переменная (локализация) | Не применимо | `pixel_offset_x`, `pixel_offset_y` или GPS-смещение |
| Способ разметки | Визуальный осмотр спектрограмм + видео + показаний газоанализатора | Автоматическая разметка по газоанализатору + верификация экспертом |

**Результат этапа:** Train/Validation/Test сплиты (70/15/15) с сохранением временной структуры (окна из одного полета не перемешиваются между train и validation).

---

### 2.3.3 Этап 3: Feature Engineering

| Подход | Бейзлайн | Основной MVP |
| :--- | :--- | :--- |
| **Акустика** | MFCC (13), спектральный центроид, ZCR, RMS | Log-mel спектрограмма (64 bins × 128 window) + cross-correlation между каналами микрофонной решетки |
| **RGB-видео** | Не используется в бейзлайне | Кадр 224×224, нормализация ImageNet, возможен pretrained backbone (EfficientNet) |
| **Тепловизионное видео** | Не используется в бейзлайне | Кадр 224×224, min-max нормализация |
| **Газоанализатор** | Не используется в бейзлайне | Сырая ppm, разность со скользящим фоном (за 10 секунд) |
| **Нормализация** | StandardScaler (per-sensor) | Instance normalization (акустика), BatchNorm (видео) |
| **Аугментация (акустика)** | Добавление белого шума (SNR=20dB) | Time-stretch (±10%), pitch-shift (±2 полутона), добавление шума ветра (реальные записи) |
| **Аугментация (видео)** | Нет | RandomHorizontalFlip, RandomBrightnessContrast, GaussianBlur |

**Результат этапа:**

| Версия | Размерность | Формат |
| :--- | :--- | :--- |
| Бейзлайн | (N_samples, 20_features) | NumPy .npy |
| Основной MVP | (N_samples, 64, 128) — акустика + (N_samples, 224, 224, 3) — RGB + (N_samples, 224, 224, 1) — thermal + (N_samples, 1) — methane | HDF5 / TFRecord |

---

### 2.3.4 Этап 4: Обучение прогнозных моделей

**Бейзлайн (Random Forest на акустике + порог на газоанализаторе):**

| Параметр | Значение |
| :--- | :--- |
| Алгоритм (акустика) | Random Forest |
| n_estimators (RF) | 200 |
| max_depth (RF) | 15 |
| class_weight (RF) | balanced |
| Газоанализатор | Порог > 10 ppm над фоном |
| Фьюжн | Логическое ИЛИ (акустика ИЛИ газ) |
| Кросс-валидация | GroupTimeSeriesSplit (5 фолдов) |

**Основной MVP (мультимодальная нейросеть):**

| Параметр | Значение |
| :--- | :--- |
| Архитектура | Мультимодальная: AudioEncoder (ResNet18 на Mel-спектрограмме) + RGBEncoder (EfficientNetB0) + ThermalEncoder (CNN 3 слоя) + MethaneEncoder (MLP) + Fusion (Transformer или Concat + MLP) |
| Вход (акустика) | (batch, 1, 64, 128) — mel-спектрограмма за 1 секунду |
| Вход (RGB) | (batch, 3, 224, 224) — кадр видимой камеры |
| Вход (thermal) | (batch, 1, 224, 224) — тепловизионный кадр |
| Вход (methane) | (batch, 1) — концентрация метана (или разность с фоном) |
| Выход 1 | Sigmoid → вероятность утечки (binary cross-entropy loss) |
| Выход 2 | Linear → смещение в пикселях / GPS-координатах (Huber loss) |
| Общий Loss | L = L_cls + 0.3 * L_loc |
| Оптимизатор | AdamW (lr=1e-3, weight_decay=1e-4) |
| Scheduler | ReduceLROnPlateau (factor=0.5, patience=3) |
| Кросс-валидация | Leave-One-Flight-Out (по полетным сессиям) |

**Результат этапа:** сохраненные веса модели (.pt) + логи обучения.

---

### 2.3.5 Этап 5: Интерпретация моделей (согласование с заказчиком)

| Метод | Для чего | Формат вывода |
| :--- | :--- | :--- |
| SHAP | Бейзлайн — определение важности акустических признаков | Summary plot, Force plot |
| Grad-CAM (акустика) | Основной MVP — визуализация важных частот | Тепловая карта на спектрограмме |
| Grad-CAM (видео) | Основной MVP — визуализация области на кадре, повлиявшей на решение | Тепловая карта на RGB / тепловизионном кадре |
| Attention weights | Основной MVP — как сенсоры взаимодействуют | Матрица внимания |
| Confusion Matrix | Оба подхода — анализ структуры ошибок | Матрица 2×2 |

**Бизнес-проверка:** эксперт-акустик подтверждает, что важные области спектра (по Grad-CAM) соответствуют физике утечки газа (спектр свиста в диапазоне 5-15 кГц). Эксперт по ТБ подтверждает, что тепловизионные аномалии соответствуют ожидаемому охлаждению.

**Результат этапа:** отчет с визуализациями + заключение экспертов.

---

### 2.3.6 Этап 6: Интеграция бизнес-правил для расчета бизнес-метрик качества модели

**Бизнес-правила поверх ML-предсказания:**

| № | Правило | Обоснование |
| :--- | :--- | :--- |
| 1 | Утечка регистрируется при 3 последовательных кадрах (или акустических окнах) с вероятностью > 0.7 | Исключение случайных выбросов |
| 2 | При скорости ветра > 10 м/с порог повышается до 0.85 (акустика зашумлена) | Физическое ограничение акустики |
| 3 | Если газоанализатор показывает > 50 ppm над фоном — утечка регистрируется немедленно (высокая надежность) | Приоритет химического подтверждения |
| 4 | Ночью (или при плохой видимости) вес тепловизионной камеры увеличивается в 2 раза | RGB-камера бесполезна |
| 5 | При расчетной утечке > 100 м³/час (по падению давления в SCADA) → автоматический вызов МЧС | Крупная авария |

**Вопрос бизнесу:** согласованы ли пороги (0.7, 0.85, 3 кадра) с регламентом ПБ 12-529-03 и с возможностями дрона?

**Результат этапа:** функция постобработки, применяемая к выходам ML-модели перед отправкой алерта в ЦДС.

---

### 2.3.7 Этап 7: Разработка оптимизатора (выбор оптимальной итерации)

**Перебираемые параметры:**

| Параметр | Диапазон | Шаг |
| :--- | :--- | :--- |
| Порог вероятности p_threshold | [0.5, 0.9] | 0.05 |
| Длительность подтверждения frames_required | [1, 5] | 1 |
| Вес газоанализатора в фьюжне | [0.5, 2.0] | 0.25 |

**Целевая функция (максимизация):**

`Score = F1_score - 0.05 * FAR`

где FAR (False Alarm Rate) — частота ложных срабатываний **на 100 км полета**.

**Бизнес-калибровка:**

| Сценарий | Стоимость |
| :--- | :--- |
| Ложный вызов (FP) | 50 000 руб (выезд бригады) |
| Пропущенная утечка (FN) | 10 000 000 руб (риск аварии) |

**Оптимальная точка (расчетная):** Recall 0.92 при Precision 0.88.

**Результат этапа:** оптимальная тройка (p_threshold, frames_required, gas_weight) на валидационной выборке.

---

### 2.3.8 Этап 8: Подготовка финального отчета для бизнеса

**Структура отчета:**

1. Резюме для руководства (ключевые метрики, готовность к пилотным полетам)
2. Метрики качества на тестовом полигоне (таблица целевых и достигнутых значений, confusion matrix по каждому сенсору и фьюжну)
3. Оценка экономического эффекта (предотвращенный ущерб в руб/год, сокращение затрат на пешие обходы)
4. Заключение экспертов (акустика, тепловизионная диагностика, газоанализ)
5. Рекомендации по порогам срабатывания (итоговые параметры для инференса на борту или на земле)
6. План интеграции в ЦДС (схема развертывания: дрон → наземная станция → ЦДС)

**Ответственные:** DS (подготовка данных и аналитики) + PO (согласование выводов) + пилот БПЛА (верификация)

**Результат этапа:** презентация (PDF/PPT) для бизнес-заказчика.

---

### Сводная таблица этапов

| Этап | Вход | Выход | Ответственный | Длительность |
| :--- | :--- | :--- | :--- | :--- |
| 2.3.1 | Сырые данные с дрона (акустика + видео + газ) + SCADA | Мультимодальная витрина данных | DS + DE + пилот | 10 дней |
| 2.3.2 | Витрина данных | Train/Val/Test сплиты | DS | 2 дня |
| 2.3.3 | Сырые данные (аудио, видео, газ) | Mel-спектрограммы, кадры, ppm-признаки | DS | 5 дней |
| 2.3.4 | Признаки + таргет | Обученные модели (по сенсорам и фьюжн) | DS | 7 дней |
| 2.3.5 | Модели | SHAP / Grad-CAM / Attention отчет | DS | 3 дня |
| 2.3.6 | Предсказания | Алерты с бизнес-правилами | DS + PO | 2 дня |
| 2.3.7 | Валидационная выборка | Оптимальные пороги и веса | DS | 2 дня |
| 2.3.8 | Все вышеперечисленное | Презентация бизнесу | DS + PO | 3 дня |
