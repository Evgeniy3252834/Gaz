## 1. Цели и предпосылки

### 1.1. Зачем идем в разработку продукта?

| Роль | Ответ |
| :--- | :--- |
| **Бизнес-цель (Product Owner)** | Снизить операционные потери газа от невыявленных утечек на **15%** и предотвратить техногенные аварии за счет перехода от планово-предупредительного ремонта к предиктивной аналитике. |
| **Почему станет лучше (ML)** | **Сейчас:** обход трасс бригадами раз в 2 недели, реагирование только на падение давления.<br>**Будет:** обнаружение микро-утечек (от 0.1 л/с) в реальном времени с локализацией до **50 метров**. Время реакции снизится с дней до минут. |
| **Успех итерации (PO)** | Precision **> 95%**, Recall **> 90%** на тестовом полигоне длиной 50 км в течение 3 месяцев без ложных срабатываний более 1 раза в неделю. |

### 1.2. Бизнес-требования и ограничения

**Бизнес-требования:**
- Соответствие ПБ 12-529-03 «Правила безопасности систем газораспределения»
- Передача сигнала в диспетчерскую ЦДС за **< 5 секунд** с момента события

**Бизнес-ограничения:**

> [!CAUTION]
> - **Запрет** на использование любых иностранных облачных серверов для обработки данных (только локальный контур или ГосСОПКА)
> - Энергопотребление полевого датчика **< 2 Вт** (работа от солнечной панели)
> - Работоспособность при температуре **от -55°C до +45°C**

**Критерии успеха пилота:**
- Модель обнаружила **3 из 4** искусственно созданных утечек на полигоне
- Частота ложных срабатываний **≤ 1 в неделю** на 10 км трассы

### 1.3. Скоуп проекта

**Входит в скоуп (DS):**
- Бинарный классификатор (утечка / шум)
- Модель регрессии для локализации места утечки (TDOA)
- Пайплайн предобработки сигнала
- One-Class SVM для обнаружения неизвестных типов утечек

**Что НЕ входит:**
- Разработка firmware для сенсоров (команда Embedded)
- UI диспетчерского пульта (фронт-команда)

**Технический долг:**
- Отсутствие механизма Continual Learning
- Хардкод частоты дискретизации (20 кГц)

### 1.4. Предпосылки решения

**Используемые блоки данных:**

| Данные | Источник | Статус |
| :--- | :--- | :--- |
| Акустические записи (размеченные) | Полигон 2022-2023 | Требуется реразметка |
| Параметры трубы (давление, диаметр) | SCADA-архив | ✅ Доступно |
| Метеоданные (ветер, осадки) | API Росгидромет | ✅ Доступно |
| Исторические акты аварий | Журнал ЦДС | ⚠️ Требуется допуск |

**Гипотезы:**
1. Спектр утечки газа имеет характерные гармоники в диапазоне **5-15 кГц**
2. Разница во времени прихода сигнала (TDOA) позволяет локализовать утечку с точностью **±50 м**

> [!IMPORTANT]
> **Вопрос бизнесу:** Насколько отличается акустический портрет утечки при низком давлении (5 атм) vs высоком (100 атм)?

---

## 2. Методология Data Scientist

### 2.1. Постановка задачи

Гибридная система, объединяющая три типа задач ML:

| Тип задачи | Применение |
| :--- | :--- |
| **Бинарная классификация** | Определение: есть утечка газа (1) или нет (0) |
| **Обнаружение аномалий** | Выявление неизвестных типов дефектов (бейзлайн) |
| **Регрессия (локализация)** | Оценка расстояния от сенсора до места утечки |

**Формат вывода (на окне 1 секунда):**
- `p_leak ∈ [0,1]` — вероятность утечки
- `distance ∈ [0, 5000]` метров — расстояние до источника

### 2.2. Блок-схема решения

#### Бейзлайн (MVP 1.0)

```mermaid
flowchart TD
    A[Сырые аудиозаписи] --> B[Предобработка<br>фильтрация + сегментация]
    B --> C[Feature Engineering<br>MFCC + спектральные признаки]
    C --> D[Обучение<br>Random Forest / One-Class SVM]
    D --> E[Оценка на тестовом полигоне]
    E --> F{Precision > 0.95<br>Recall > 0.90?}
    F -- Нет --> C
    F -- Да --> G[Экспорт в ONNX<br>пилот на 2 контроллерах]
